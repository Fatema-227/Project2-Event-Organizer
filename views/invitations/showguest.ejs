<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Show Invitation</title>
</head>
<body class="guest-invitation-body">

  <div class="invitation-flip-container">
    <div class="invitation-card">
      <div class="invitation-card-front"></div>

      <div class="invitation-card-back">
        <form action="/invitations/showguest/<%= invitations._id %>" method="post" class="invitation-response-form">
          <h2 class="invitation-title"><%= invitations.event_id.title %></h2>
          <div class="card-back-info">
            <p class="invitation-description"><%= invitations.event_id.description %></p>
            <p class="invitation-date"><%= invitations.event_id.date %></p>
            <p class="invitation-time"><%= invitations.event_id.time %></p>
            <p class="invitation-address"><%= invitations.event_id.address %></p>
          </div>


          <% if (!invitationResponse) { %>
            <label for="invitationStatus" class="invitation-label">Will you attend?</label>
            <select name="invitationStatus" id="invitationStatus" class="invitation-select" required>
              <option value="Accepted">Accepted</option>
              <option value="Declined">Declined</option>
            </select>

            <label for="comment" class="invitation-label">Comment</label>
            <textarea name="comment" id="comment" rows="3" cols="25" class="invitation-textarea" required></textarea>

            <button type="submit" class="invitation-submit-button">Send Response</button>
            <p class="invitation-note">*Please note: You can't change your response.</p>
          <% } %>
        </form>
      </div>
    </div>
  </div>

</body>
</html>
